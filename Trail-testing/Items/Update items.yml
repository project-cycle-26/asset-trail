info:
  name: Update items
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:3000/api/items/1
  body:
    type: json
    data: |-
      {
        "name": "DC Motorss"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
