info:
  name: Post members
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/members
  body:
    type: json
    data: |-
      {
        "name": "Admin1",
        "email": "admin1@example.com",
        "password": "admin123",
        "role": "ADMIN"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
