info:
  name: Patch Member
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:3000/api/members/2
  body:
    type: json
    data: |-
      {
        "name": "Core Team Lead",
        "role": "CORE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
