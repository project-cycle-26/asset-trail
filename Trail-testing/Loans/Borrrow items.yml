info:
  name: Borrrow items
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/loans
  body:
    type: json
    data: |-
      {
        "item_id": 5,
        "member_id": 5,
        "due_date": "2026-03-12",
        "purpose": "Project work"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
