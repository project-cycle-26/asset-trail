info:
  name: Get member loan
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/api/loans?member_id=4
  params:
    - name: member_id
      value: "4"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
